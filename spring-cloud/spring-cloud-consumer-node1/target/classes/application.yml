server:
  port: 9001
spring:
  application:
    name: node1
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka/
  instance:
    prefer-ip-address: true
feign:
  hystrix:
    enabled: true